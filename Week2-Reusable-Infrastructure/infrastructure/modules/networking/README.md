# Networking Module

Creates subnets, route tables, internet gateway, and optional NAT gateway(s).

## Features
- Multi-AZ public/private subnets
- Public and private route tables
- Toggle NAT creation with `enable_nat_gateway`
- Option for single or per-AZ NAT via `single_nat_gateway`
